{{- if .Values.prometheus.enable }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "perses-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    control-plane: controller-manager
  name: {{ include "perses-operator.resourceName" (dict "suffix" "controller-manager-metrics-monitor" "context" $) }}
  namespace: {{ .Release.Namespace }}
spec:
  endpoints:
    - path: /metrics
      port: https
      scheme: https
      bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
      tlsConfig:
        {{- if .Values.certManager.enable }}
        serverName: {{ include "perses-operator.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ .Release.Namespace }}.svc
        insecureSkipVerify: false
        ca:
          secret:
            name: {{ include "perses-operator.resourceName" (dict "suffix" "webhook-server-cert" "context" $) }}
            key: ca.crt
        cert:
          secret:
            name: {{ include "perses-operator.resourceName" (dict "suffix" "webhook-server-cert" "context" $) }}
            key: tls.crt
        keySecret:
          name: {{ include "perses-operator.resourceName" (dict "suffix" "webhook-server-cert" "context" $) }}
          key: tls.key
        {{- else }}
        insecureSkipVerify: true
        {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "perses-operator.name" . }}
      control-plane: controller-manager
{{- end }}
