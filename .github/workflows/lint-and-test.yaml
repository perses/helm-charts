name: lint and test

on:
  pull_request:
    paths:
      - "charts/**"
    branches:
      - main

jobs:
  helm-lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: load env
        run: cat .github/env >> $GITHUB_ENV

      - name: setup helm lint requirements
        uses: ./.github/actions/setup_helmlint
        with:
          create-kind-cluster: "true"
          kind-version: ${{ env.kind-version }}
          kind-image: ${{ env.kind-image }}

      - uses: perses/github-actions@v0.11.0
      - uses: ./.github/perses-ci/actions/setup_environment
        with:
          enable_go: true

      - name: run chart-testing (install)
        run: ct install --all
      - name: install mdox
        run: go install github.com/bwplotka/mdox@latest

      - name: run chart README lint
        run: make update-helm-readme && make checkdocs
